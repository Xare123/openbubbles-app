name: media_kit_libs_windows_video
description: Auditable OpenBubbles Windows native media bundle for package:media_kit.
version: 1.0.11+openbubbles.1
publish_to: none
homepage: https://github.com/OpenBubbles/openbubbles-app
repository: https://github.com/OpenBubbles/openbubbles-app

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: MediaKitLibsWindowsVideoPluginCApi
